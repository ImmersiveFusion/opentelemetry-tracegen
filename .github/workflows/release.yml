name: Release

on:
  push:
    tags:
      # Accept both the bare-number form (0.1.0 through 0.7.1) and the
      # conventional v-prefixed form (v0.7.2 onward). The original filter
      # matched only the bare form, so the v-prefixed v0.7.2 release tag
      # silently never triggered a run.
      - '[0-9]+.[0-9]+.[0-9]+'
      - 'v[0-9]+.[0-9]+.[0-9]+'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - goos: linux
            goarch: amd64
            suffix: linux-amd64
          - goos: linux
            goarch: arm64
            suffix: linux-arm64
          - goos: windows
            goarch: amd64
            suffix: windows-amd64.exe
          - goos: darwin
            goarch: arm64
            suffix: darwin-arm64
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Build
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: go build -ldflags="-s -w" -o snowglobe-${{ matrix.suffix }} ./cmd/snowglobe

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: snowglobe-${{ matrix.suffix }}
          path: snowglobe-${{ matrix.suffix }}

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          merge-multiple: true

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          files: snowglobe-*
          generate_release_notes: true

  # Build + push the multi-arch container image to Docker Hub. Published to BOTH
  # immersivefusion/snowglobe (canonical) and immersivefusion/tracegen (legacy alias),
  # identical manifests, so existing `docker run immersivefusion/tracegen` keeps working.
  # Runs on the same version-tag trigger as the binaries above. Independent of `build`.
  # Requires repo secret: DOCKERHUB_TOKEN (username is the public namespace, hardcoded below).
  docker:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-qemu-action@v3

      - uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: immersivefusion   # public namespace, not a secret
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Docker metadata (tags + labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            immersivefusion/snowglobe
            immersivefusion/tracegen
          tags: |
            type=semver,pattern={{version}}
            type=raw,value=latest

      - name: Build and push (linux/amd64 + arm64)
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            VERSION=${{ steps.meta.outputs.version }}
